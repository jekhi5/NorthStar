import express from 'express';

const notificationController = () => {
  const router = express.Router();

  // Controller for notification based operations. Will be useful when sending notifications to Users in the future.

  return router;
};

export default notificationController;
